import React from "react";

const SectionContent = ({ children }) => {
  return (
    <div className="mb-10 text-2xl font-serif text-gray-700 dark:text-gray-200">
      {children}
    </div>
  );
};

export default SectionContent;
